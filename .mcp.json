{
  "mcpServers": {
    "salesforce-dx": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs",
        "meridian",
        "--toolsets",
        "data,metadata,testing",
        "--no-telemetry"
      ]
    }
  }
}
